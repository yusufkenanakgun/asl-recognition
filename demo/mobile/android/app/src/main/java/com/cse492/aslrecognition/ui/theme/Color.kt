package com.cse492.aslrecognition.ui.theme

import androidx.compose.ui.graphics.Color

// Mockup palette (design/screens.jsx) — teal accent, light theme only.
object AppColors {
    // Surfaces
    val Background = Color(0xFFFFFFFF)
    val Surface = Color(0xFFF7F8F8)
    val CardBorder = Color(0xFFECEEF0)
    val Divider = Color(0xFFEEF0F2)

    // Text
    val TextPrimary = Color(0xFF101418)
    val TextSecondary = Color(0xFF5A6066)
    val TextTertiary = Color(0xFFB8BDC2)

    // Teal accent family
    val Teal = Color(0xFF00897B)
    val TealDark = Color(0xFF00695C)
    val TealContainer = Color(0xFFB2DFDB)
    val TealSoft = Color(0xFFE0F2F1)
    val TealTint = Color(0xFFF2F9F8)
    val OnTeal = Color(0xFFFFFFFF)
}
