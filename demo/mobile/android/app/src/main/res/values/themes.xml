<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Manifest tarafından referans edilen taban tema. Compose Material 3
         tema kodda kuruluyor; bu sadece Activity öncesi durum çubuğu /
         pencere arka planı için. -->
    <style name="Theme.ASLRecognitionDemo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
